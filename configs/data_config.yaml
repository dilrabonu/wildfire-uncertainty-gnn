data:
  raw_dir: data/raw
  interim_dir: data/interim
  processed_dir: data/processed
  metadata_file: data/external/metadata.json

graph:
  neighborhood: 8
  include_self_loops: false

target:
  type: burn_probability